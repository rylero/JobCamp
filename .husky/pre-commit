#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running linting..."
pnpm lint

echo "🧪 Running quick tests..."
pnpm test:run --reporter=verbose --run

echo "✅ Pre-commit checks passed!"
